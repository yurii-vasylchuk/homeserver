apiVersion: apps/v1
kind: Deployment
metadata:
  name: transmission-flood
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transmission-flood
  template:
    metadata:
      labels:
        app: transmission-flood
    spec:
      containers:
        - name: transmission
          image: {{ .Values.transmission.image }}
          ports:
            - containerPort: {{ .Values.transmission.rpcPort }}
          env:
            - name: USER
              value: "{{ .Values.transmission.env.USER }}"
            - name: PASS
              value: "{{ .Values.transmission.env.PASS }}"
          volumeMounts:
            - name: data
              mountPath: /downloads

        - name: flood
          image: {{ .Values.flood.image }}
          ports:
            - containerPort: {{ .Values.flood.port }}
          env:
            - name: FLOOD_OPTION_host
              value: "0.0.0.0"
            - name: FLOOD_OPTION_port
              value: "{{ .Values.flood.port }}"
            - name: FLOOD_OPTION_trurl
              value: "localhost:{{ .Values.transmission.rpcPort}}/transmission/rpc"
            - name: FLOOD_OPTION_truser
              value: "{{ .Values.transmission.env.USER }}"
            - name: FLOOD_OPTION_trpass
              value: "{{ .Values.transmission.env.PASS }}"
            - name: FLOOD_OPTION_auth
              value: none
          volumeMounts:
            - name: data
              mountPath: /downloads

      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ .Values.persistence.pvcName }}
