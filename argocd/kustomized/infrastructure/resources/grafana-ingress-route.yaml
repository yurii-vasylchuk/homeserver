apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
  namespace: infrastructure
spec:
  entryPoints:
    - websecure
  routes:
    - match: "Host(`grafana.vasylchuk.org.ua`) && PathPrefix(`/`)"
      kind: Rule
      services:
        - name: grafana
          port: 80
  tls:
    certResolver: letsencrypt

---

apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: redirect-http-grafana
  namespace: infrastructure
spec:
  entryPoints:
    - web
  routes:
    - match: "Host(`grafana.vasylchuk.org.ua`) && PathPrefix(`/`)"
      kind: Rule
      middlewares:
        - name: redirect-to-https
          namespace: kube-system
      services:
        - name: noop-svc
          namespace: kube-system
          port: 80
