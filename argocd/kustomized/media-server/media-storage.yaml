apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-pvc
  namespace: media-server
spec:
  storageClassName: longhorn-hdd
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
